import {StyleSheet} from 'react-native';
import Common from '@Common';

export default StyleSheet.create({
  noRecordText: {
    fontSize: 18,
    color: "#e91e63",
    marginTop: 20,
    alignSelf: 'center',
    fontWeight: 'bold'
  },
});
